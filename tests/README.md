# Cross-Cutting Integration Tests

> **Status:** Placeholder — end-to-end tests will live here as the platform matures.

## Purpose

This directory is reserved for integration tests that span multiple packages (e.g., backend + database + Redis via Docker Compose).

## Current State

- Backend unit/integration tests: `backend/tests/`
- Frontend component tests: `frontend/src/**/*.test.tsx`

## Future Work

<!-- TODO: docker-compose test profile -->
<!-- TODO: API contract tests across services -->
<!-- TODO: Smoke tests for CI post-deploy -->
